# План доведения до «Полностью готов к работе» (v1.0)

Дата: 2026-07-01. База: ветка `claude/repo-exploration-4o50if` (PR #1 — improvement pack).
Формат: контракт приёмки + фазы «шаг → verify». Прогоны и артефакты — только в полигон
(`D:\AI-PROJECTS\WORKFLOW-PLANNER-TEST`), не в репозиторий.

## Контракт приёмки «полностью готов»

- [ ] **Живые тесты.** Все однотуровые фикстуры PASS через `tools/run-fixtures.js` с реальным
  `claude -p` (вердикт судьи + ручная выборка: каждый FAIL и один случайный PASS);
  все multi-turn фикстуры PASS вручную по ранбуку. Транскрипты в полигоне, с хэшем коммита.
- [ ] **Review-workflow доказан.** Один живой прогон `templates/review-prompt.js` на промпте
  с заранее заложенными дырами: находит все заложенные, вердикт по шкале
  ready / needs fixes / needs rebuilding.
- [ ] **Реестр фактов датирован.** Обе эмпирические строки реестра
  (`reference/workflow-primitives.md`) подтверждены свежей пробой с датой и версией Claude Code.
- [ ] **Codex-паритет.** Pending-список в `PARITY.md` закрыт или явно принят как исключение v1;
  `node tools/check-parity.js` — 0 missing.
- [ ] **Установка с нуля.** На чистом профиле `/skills` видит скил; демо-задача даёт план и
  скрипт; валидатор запускается через разрешение `Bash(node:*)`.
- [ ] **Релиз.** PR #1 смержен, `CHANGELOG.md` заполнен, тег `v1.0.0`.

## Фаза 1 — влить базу

1. Ревью PR #1, правки по замечаниям → verify: PR смержен, main зелёный.

## Фаза 2 — Codex-паритет (закрыть Pending)

2. Порт review-workflow в форму Codex-рантайма (главный агент + 6 плоских скептиков, без JS);
   обновить run modes и глоссарий codex `prompt-helper.md` → verify: линзы 1:1 с Claude-версией.
3. Codex-аналог `after-run.md` (перезапуск упавших сабагентов; без resume-семантики — её в
   Codex нет, это честное различие) → verify: ни одного Claude-специфичного термина.
4. Порт примеров «якорь phase-0» и «цикл до сухого» в codex `examples.md` (форма плана, не
   скрипта) → verify: соответствие рецептам 3 и 8.
5. Scale & budget в `templates/codex-workflow-plan.md` (предпочтение 2-6 сабагентов, без
   лимита 16; потолок стоимости согласуется с пользователем) → verify: числа из
   `codex-workflow-runtime.md`, не из Claude-версии.
6. Порт `tests/gate/fixtures.md` (script-claims переформулированы: без JS, без валидатора;
   G9 = «запуск только после явного одобрения») + раздел автопрогонов в codex-ранбук
   (`--cmd "codex exec"`) → verify: ни одной ссылки на `.claude/workflows` в codex-сюите.
7. Обновить `PARITY.md` (карта + чистка Pending), пары в `check-parity.js`, версия
   codex-плагина 0.1.0 → 0.2.0, «Node ≥ 18 для tools/» в README EN/DE
   → verify: `check-parity.js` 0 missing / 0 drifted; Pending пуст.

## Фаза 3 — живые прогоны (полигон; самая важная фаза)

8. Пробы реестра (~4 агента, процедура в `workflow-primitives.md`) → verify: обе строки
   датированы, версия Claude Code записана.
9. Headless: `node tools/run-fixtures.js --suite tests/prompt-helper/fixtures.md --out <полигон> --judge`
   и то же для `tests/gate/fixtures.md` → verify: сводка PASS; каждый FAIL спот-чекнут руками.
10. Ручные multi-turn — первыми два критических: **G9** (весь путь план → одобрение → скрипт →
    валидатор) и **F13** (негатив согласия); затем остальные → verify: транскрипты по шаблону
    ранбука, evidence-поля заполнены.
11. Живой прогон review-prompt.js на «дырявом» промпте (заложить: слепую автономию на
    destructive-шаге, verify-by-count, общий источник без якоря) → verify: все три дыры в
    вердикте.
12. Разбор транскриптов, починка регрессий → verify: повторный прогон упавших — PASS.

## Фаза 4 — релиз

13. Чистая установка на втором профиле, демо-задача → verify: план + скрипт получены,
    валидатор отработал.
14. `CHANGELOG.md`, тег `v1.0.0` → verify: тег на смерженном main.

## Риски

- **Главный:** фикстуры ещё ни разу не гонялись на живом скиле — фаза 3 может вскрыть
  регрессии формулировок SKILL.md (маршрутизация, ратификация). Закладка: п. 12.
- Headless-режим может не активировать скил из описания — тогда во входы фикстур добавляется
  явный вызов `/workflow-planner` (однострочная правка сюит).
- `codex exec` может не читать промпт со stdin — тогда для codex-сюиты остаётся ручной ранбук
  (в раннере это уже задокументировано как требование к `--cmd`).
